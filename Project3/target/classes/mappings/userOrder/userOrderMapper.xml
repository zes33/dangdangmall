<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="userOrderDAO">

	<!-- 마이페이지 -> 내 주문 내역 출력 시 사용 -->
	<select id="getMyOrder" parameterType="com.spring.mall.order.vo.UserOrderVO"
			resultType="com.spring.mall.order.vo.UserOrderVO">
		SELECT ORDER_ID, ORDER_DATE, ORDER_ADDR, ORDER_ADDR_D, 
        		ORDER_ZIPCODE, ORDER_RECEIVER, ORDER_PHONE, 
        		ORDER_TOTAL, USER_STATUS, USER_ID 
    		FROM USER_ORDER
    		WHERE USER_ID = #{user_id}
	</select>
 
</mapper>