<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="userDAO">

	<!-- 로그인 -->
	<select id="getUser" parameterType="com.spring.mall.user.vo.UserVO" 
			resultType="com.spring.mall.user.vo.UserVO">
			SELECT USER_ID, USER_PW, USER_NAME, USER_NICKNAME, USER_EMAIL, USER_GRADE,
				 USER_POINT, USER_ADDR, USER_ADDR_D, USER_ZIPCODE, USER_STATE, USER_PHONE1,
				 USER_PHONE2, USER_PHONE3
			FROM USERS
			WHERE USER_ID = #{user_id} AND USER_PW = #{user_pw}
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertUser" parameterType="com.spring.mall.user.vo.UserVO">
		INSERT INTO USERS
    		(USER_ID, USER_PW, USER_NAME, USER_NICKNAME, USER_EMAIL, USER_PHONE1,
    		 USER_PHONE2, USER_PHONE3)
    		VALUES(#{user_id},#{user_pw},#{user_name},#{user_nickname},#{user_email},
    		#{user_phone1}, #{user_phone2}, #{user_phone3})
	</insert>
	
	<!-- 아이디 찾기 -->
	<select id="getId" parameterType="com.spring.mall.user.vo.UserVO" 
			resultType="com.spring.mall.user.vo.UserVO">
		SELECT USER_ID FROM USERS
		WHERE USER_EMAIL = #{user_email}
	</select>
	
	<!-- ID 중복 체크 -->
	<select id="idCheck" resultType="int">
		<![CDATA[
			SELECT count(*) FROM USERS
			WHERE user_id = #{user_id}
		]]>
	</select>
	
	<!-- 회원 탈퇴 -->
	<update id="secession">
		UPDATE USERS
		    SET USER_STATE = 0
		    WHERE USER_ID = #{user_id}
		    AND USER_PW = #{user_pw}
	</update>

</mapper>